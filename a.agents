{
  "about": "Agent Council demo. Runs a 5-step flow: (1) ingest question+context, (2) build personas, (3) edit council, (4) parallel answers, (5) peer review + chairman synthesis. Requires OPENAI_API_KEY.",
  "entrypoint": "python3 test_flow_full.py",
  "env": {
    "OPENAI_API_KEY": "set-your-key-here"
  },
  "notes": [
    "Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt && pip install -e .",
    "Outputs: logs/*.md (prompts/responses/tokens), council_session_complete.json (results).",
    "Config: personas are generated automatically; you can edit them in step 3."
  ]
}

